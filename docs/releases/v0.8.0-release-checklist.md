# YiLianQianYan v0.8.0 Release Checklist

This checklist records evidence for the v0.8 release gate. `NOT TESTED` is not a pass.

## Gate 0 — consistency and security boundary

- [x] Process grants and the Grant Editor expose only `ManagedChildren`.
- [x] New `ExplicitPid` and `AllHostProcesses` grants are rejected; legacy rows fail closed.
- [x] Runtime process controls reject unknown PIDs and the backend's own PID.
- [x] Network and process authorization evidence is created by the Security Execution Gateway and passed through `ToolExecutionContext`.
- [x] Network DNS candidates are checked against the trusted zone evidence after one resolution; redirects and proxy use are disabled.
- [x] Runtime audit events preserve the request subject ID, including `subject-test-123` regression coverage.
- [ ] Real packaged Windows UI/API network and process isolation — NOT TESTED; packaged executable launch was blocked by the current agent execution policy.

## Version and artifacts

- [x] Backend, Tauri, frontend, lockfile, health version, and release documents target `0.8.0`.
- [x] `CHANGELOG.md` contains Added, Security, and Known Boundaries sections.
- [x] NSIS release bundle generated: `target/release/bundle/nsis/忆涟千言_0.8.0_x64-setup.exe`.
- [x] Installer SHA-256: `B22D50631675DFBA0B4CFF91CBD2257AF6233C3776BCEA8BE7C7E483BD345D11`.
- [x] Silent install/uninstall smoke completed in an isolated temporary directory; exit code `0`, install directory removed afterward.

## Build and tests

- [x] `cargo fmt --all -- --check` — passed.
- [x] `cargo check --workspace --locked` — passed.
- [x] `cargo test --workspace --locked` — passed: backend `669`, Tauri `5`, security execution `10`, runtime smoke `1`.
- [x] `npm.cmd ci` — passed; installed 297 packages.
- [x] `npm.cmd test -- --run` — passed: 13 files, 83 tests.
- [x] `npm.cmd run build` — passed; Vite reported one chunk over 500 KB.
- [ ] `cargo audit` — NOT TESTED; `cargo-audit` was installed, but the RustSec advisory database fetch failed due to a Git/Schannel transport decode error.
- [ ] `npm.cmd audit --omit=dev` — findings are detailed below: 5 moderate, 0 high, 0 critical; fixes require breaking major upgrades.

## Runtime and data safety

- [x] Fresh isolated profile backend smoke: schema initialized, health `healthy`, version `0.8.0`, policy `security-rbac-v3`, one owner role, two default grants, and six built-in workflows.
- [x] Fresh second-start idempotence: the owner, grant, subject, and workflow counts remained unchanged; no migration error occurred.
- [x] Legacy upgrade fixture: conversation, memory, and workflow preserved; `env_secret_refs` added; model, embedding, and MCP plaintext migrated to SecretStore; restart remained idempotent.
- [x] Real Windows Credential Manager smoke through the production `OsSecretStore`: write, rotate, restart resolution, and clear passed; `cmdkey` confirmed credential presence/removal without exposing values.
- [x] Secret sentinel scan: API response, SQLite settings/MCP rows, and audit-visible paths contained zero test sentinel matches.
- [ ] SQLite backup/rollback and downgrade execution — NOT TESTED; release notes require backup before upgrade and do not claim downgrade support.

## Desktop hard gate

- [ ] Real Tauri development run with a visible non-blank window — NOT TESTED.
- [ ] Packaged close/restart lifecycle with no orphan `yilian-server`/PowerShell/MCP processes — NOT TESTED.
- [x] Standalone isolated backend close smoke: `yilian-server` exited and port `9421` was released after Ctrl+C.
- [ ] Real Bash timeout/privilege/registry checks — NOT TESTED.
- [x] Release package builds: NSIS target passed. MSI was attempted but WiX code page 1252 cannot encode the Chinese product name; NSIS is the selected v0.8 target.
- [x] Packaged installer static install/uninstall smoke passed.
- [ ] Packaged application launch, health, close, and restart — NOT TESTED; the execution policy rejected starting the installed executable from the shell tool.

## RC runtime evidence outside the packaged GUI

- [x] Isolated backend health on loopback: HTTP 200 with `status=healthy`, `service=yilian-backend`, `version=0.8.0`, `database=healthy`, and `policy_version=security-rbac-v3`.
- [x] Local bind check: test backend listened on `127.0.0.1` only.
- [x] Existing locked backend suite covers loopback grants, public/private DNS enforcement, redirect/proxy resistance, MCP gateway decisions, process self-PID/unknown-PID denial, and recovery; these are not a substitute for packaged GUI acceptance.
- [ ] Packaged frontend UX, visible permissions state, chat, Memory, Plugin/MCP, and Workflow smoke — NOT TESTED.

## Dependency and package scan

- [ ] `cargo audit` — NOT TESTED: `cargo-audit v0.22.2` installed, but RustSec advisory database fetch failed because the Git/Schannel connection could not decode the server response.
- [ ] `npm.cmd audit --omit=dev` — 5 moderate findings, 0 high, 0 critical. Production reachability is confirmed for `react-router-dom@6.30.4` and `react-syntax-highlighter@15.6.6`; fixes require major upgrades to React Router 7.18.2 and syntax highlighter 16.1.1. No upgrade was made in the frozen v0.8 gate; track as a v0.9 dependency item.
  - `GHSA-x7hr-w5r2-h6wg`: nested `prismjs@1.27.0` via `refractor` → reachable through syntax highlighting; major fix required; accepted known risk for v0.8.
  - `GHSA-wrjc-x8rr-h8h6`, `GHSA-337j-9hxr-rhxg`: `react-router` → reachable through the production router; React Router 7 major fix; accepted known risk for v0.8.
  - `GHSA-jjmj-jmhj-qwj2`: direct `react-router-dom@6.30.4` → reachable through production navigation; React Router 7 major fix; accepted known risk for v0.8.
- [x] Installed package content: only the application and uninstaller were present; no `.env`, test DB, private key, or RC sentinel was found.
- [x] Exact project-path scan: no project checkout path found in the installed package. Compiler diagnostic/source-path strings containing dependency cache metadata were classified as non-UI build metadata; no separate PDB/source-map file shipped.

## Gate decision

Current document status: **NOT READY**. The remaining hard blocker is real packaged Tauri launch/visible-window/frontend/health/lifecycle acceptance; the current shell execution policy prevented that launch, so this is not reported as an application crash.
