# YiLianQianYan v0.9.0 Release Checklist

本清单只记录真实证据。未执行的项目保持未勾选；自动化通过不能替代最终 GUI 人工验收。

## Release metadata

- [x] Root npm package version is `0.9.0`.
- [x] Frontend package version is `0.9.0`.
- [x] Backend and Tauri Cargo package versions are `0.9.0`.
- [x] Tauri configuration version is `0.9.0`.
- [x] Cargo lock workspace entries are `0.9.0`.
- [x] MCP client metadata derives from `CARGO_PKG_VERSION` where used by runtime code.
- [x] Default Tauri bundle target is Windows NSIS only.

Evidence: `scripts/verify-release-version.ps1 -ExpectedVersion 0.9.0` passed on 2026-08-22.

## Baseline tests

- [x] Frontend: 55 files, 261 tests passed.
- [x] Tauri: 5 tests passed.
- [x] Backend library: 727 tests passed.
- [x] Runtime smoke: 1 test passed.
- [x] Security execution: 10 tests passed.
- [x] MCP targeted regression: 33 tests passed.
- [x] Rust formatting check passed after applying `cargo fmt --all`.

Known existing Rust test warnings:

- `backend/src/server.rs`: unused test import.
- `backend/src/api/memories.rs`: unused test variable.

## Build and artifact

- [x] `npm run build:windows` completed all release gates.
- [x] NSIS installer exists and has non-zero size.
- [x] SHA-256 checksum file exists and matches the installer.
- [x] Isolated silent install passed.
- [x] Installed package contains the application and uninstaller.
- [x] Installed package contains no `.env`, database, PDB, PEM, or private-key file.
- [x] Isolated silent uninstall passed and removed the application.
- [x] Installer filename, size, and SHA-256 are recorded below.

Artifact evidence:

- Filename: `忆涟千言_0.9.0_x64-setup.exe`
- Size: `7,202,138` bytes
- SHA-256: `E4C4329803B5346720C2A3C6E8666C46DBC23655C34CF320EAD3A7FEA82C5D06`
- Authenticode: `NotSigned`
- Application FileVersion: `0.9.0`
- Application ProductVersion: `0.9.0`
- Release profile build duration: `20m 49s`

## Dependency audit

- [x] Root packaging dependencies: `npm ci` reported 0 vulnerabilities.
- [x] Frontend production dependency audit recorded: 5 moderate, 0 high, 0 critical after upgrading `@tauri-apps/plugin-shell` to 2.3.5.
- [x] RustSec audit recorded: `h2` upgraded to 0.4.16; no non-ignored vulnerabilities remain.

The remaining frontend findings are the React Router 6 and `react-syntax-highlighter` / nested PrismJS chains. Available complete fixes require major-version migrations and remain documented follow-up work rather than an unreviewed release-freeze upgrade.

The Rust audit command explicitly ignores `RUSTSEC-2026-0194` and `RUSTSEC-2026-0195`. Both findings apply to `quick-xml@0.30.0`, which is present only as the build dependency of Linux `xcb` through `xcap`; it is not part of the Windows x64 runtime dependency graph or installed NSIS payload. The audit also reports 17 allowed unmaintained/unsound warnings in cross-platform transitive dependencies.

## Automated installer smoke

- [x] `scripts/test-windows-installer.ps1` completed with exit code 0.
- [x] Smoke install used a unique validated directory below the system temp directory.
- [x] Smoke cleanup touched only that validated temporary directory.

## Manual packaged Windows acceptance

- [ ] Installer UI displays the correct product name, version, and icon.
- [x] Installed application opens a visible, non-blank Tauri window.
- [x] `/api/health` returns healthy with version `0.9.0`.
- [ ] Existing user settings and data remain available after upgrade.
- [ ] New task, chat streaming, Stop, Tool execution, approval, error, and history loading work.
- [ ] Website and GUI application launch visibly focus the requested target.
- [ ] Notepad text task does not complete until text input is verified.
- [x] Application accepts a normal close request and releases backend port `9420` without an orphan process.
- [ ] Restart preserves data and settings.
- [x] Uninstall completes successfully.

Packaged GUI evidence from `scripts/test-windows-gui.ps1`:

- Window title: `忆涟千言`
- Window size: `1214x838`
- Health: `healthy`, service `yilian-backend`, version `0.9.0`, database `healthy`
- Non-blank screenshot check: 900 sampled colors and 98.2% non-dark pixels
- Visual inspection: Cyrene home, character, Composer, task history, and navigation rendered correctly
- Screenshot: `%TEMP%\YiLianQianYan-v0.9.0-gui-acceptance.png`

The idle header label `未连接` reflects the absence of an active Agent SSE run (`AgentRunState.connection=idle`); it is not the backend health indicator.

## Signing and publication

- [x] Release notes state that the current local artifact is unsigned.
- [ ] Authenticode signing completed.
- [ ] Signature verified with Windows tooling.
- [ ] Public release tag created.
- [ ] Release assets uploaded.

## Gate decision

Current status: **RELEASE CANDIDATE BUILT — CORE USER ACCEPTANCE PENDING**.

The gate may be marked **READY** only after the final NSIS build, checksum, isolated install/uninstall smoke, production dependency audit, and manual packaged Windows acceptance are recorded.
